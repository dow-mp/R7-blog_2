<h1><%= @page.title %></h1>

<p><%= @page.body %></p>

<%= link_to 'Edit', edit_page_path %>
<%= link_to 'Delete', @page, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %> <!-- the turbo_method argument name here refers to the HTTP request method (i.e. get, post, delete)--> 
<%= link_to 'Back', pages_path %>
