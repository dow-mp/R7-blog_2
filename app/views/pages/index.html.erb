<h1>Blog Pages</h1>

<ul>
<% @pages.each do |page| %>
    <!-- This method of linking is error prone and tedious, instead create a 'path helper' with rails using the as: keyword when writing the route -->
    <!-- <p><%# link_to page.title, "/pages/#{page.id}" %> </p> -->

    <!-- when you have created a path helper, use either of these methods for linking to the path: --> 
    <!-- <p><%#link_to page.title, page_path(page) %> </p> -->
    <li><p><%= link_to page.title, page %></p></li>  <!-- This will be the simplest method, and easiest to read. --> 
<% end %>
</ul>

<!-- <p><%# link_to 'Create a new page', new_page_path %> </p> --> <!-- This is a call to the path helper method: new_page_path. There are no arguments to pass in here because we are not passing any data to be displayed on the new page. --> 
<p><%= link_to 'Create a new page', new_page_path %></p>

<p><%= link_to 'Home', posts_path %></p>